/// <reference types="react" />
import React from 'react';
import { KorosType } from "./components/Koros/index";
type SectionColor = 'primary' | 'secondary' | 'tertiary' | 'plain';
type SectionProps = React.PropsWithChildren<{
    color?: SectionColor;
    className?: string;
    korosType?: KorosType;
}>;
declare const Section: ({ children, className, color, korosType }: SectionProps) => JSX.Element;
export { SectionColor, SectionProps, Section };
